import classes from './Front.module.css'

const Front = (props) => {

    return(
        <div>
            <h1 className={classes.type}>Welcome to Movie App </h1>

        </div>
    )
}

export default Front;